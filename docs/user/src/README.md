# HDSL 用户手册

HDSL 为每个 DeepSeek Harness 实例保存独立的版本、工作目录和用户数据。当前为开发预览版；仓库已提供 Windows 与 Linux 打包脚本，公开发行尚未发布。

从左侧导航进入安装、实例、插件、设置和日志页面。一个实例对应一个固定 Harness 版本；升级通过创建新实例完成。HDSL 不导入旧版 Java HDSL 的数据。

模型路由、API Key 与端点由每个实例的 Harness 本体管理，请在启动实例后的 Harness 网页中设置；启动器只在展示日志时对已知密钥做脱敏。请自行备份重要的实例数据。
